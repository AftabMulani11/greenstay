# Template only. Copy to secret.yaml (gitignored) and fill in real values,
# or on EKS prefer IRSA and delete the static keys entirely.
apiVersion: v1
kind: Secret
metadata:
  name: greenstay-aws
  namespace: greenstay
  labels:
    app.kubernetes.io/part-of: greenstay
type: Opaque
stringData:
  AWS_ACCESS_KEY_ID: "REPLACE_ME"
  AWS_SECRET_ACCESS_KEY: "REPLACE_ME"
  SQS_QUEUE_URL: "https://sqs.eu-west-1.amazonaws.com/REPLACE_ME/greenstay-email-queue"
