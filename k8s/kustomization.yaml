apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - configmap.yaml
  - backend.yaml
  - frontend.yaml
  - worker.yaml
  - hpa.yaml
  - ingress.yaml
# secret.yaml is applied separately (copy secret.example.yaml, fill in, keep gitignored)
